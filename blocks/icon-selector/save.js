/**
 * WordPress dependencies
 */

const Save = () => {
	// This block uses dynamic rendering (server-side rendering via PHP template)
	// so the save function should return null
	return null;
};

export default Save;
